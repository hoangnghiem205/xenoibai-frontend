'use strict';

describe('myApp.price module', function() {

  beforeEach(module('myApp.price'));

  describe('price controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var priceCtrl = $controller('PriceCtrl');
      expect(PriceCtrl).toBeDefined();
    }));

  });
});